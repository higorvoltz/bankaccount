/*
package com.example.bankaccount.model

@Entity
data class Client(
    val id: Long,
    val name: String,
    val idRegister: String,
    val addressClient: AddressClient?,
    val contactClient: ContactClient?
): Authenticable{
    override fun authenticate(password: Int): Boolean {
        when (password){
            password -> {
                return true
            }
            else -> return false
        }
    }

}*/
